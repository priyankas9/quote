import React from 'react';
import RandomQuoteMachine from './RandomQuoteMachine'; // Assuming RandomQuoteMachine.js is in the same directory

const App = () => {
  return (
    <div className="App">
      <RandomQuoteMachine />
    </div>
  );
};

export default App;
